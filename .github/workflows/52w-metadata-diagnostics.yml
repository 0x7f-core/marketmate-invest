name: 52w metadata diagnostics
on:
  push:
    paths:
      - .github/workflows/52w-metadata-diagnostics.yml
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - shell: bash
        run: |
          set -euo pipefail
          for url in             'https://stock.naver.com/api/domestic/detail/005930/siseDay?pageSize=5&bizdate=20260918'             'https://stock.naver.com/api/domestic/detail/005930/siseDay?pageSize=5&bizdate=20260918&tradeType=NXT'             'https://stock.naver.com/api/domestic/detail/005930/siseDay?pageSize=5&bizdate=20260918&codeType=NXT'
          do
            echo "===== $url ====="
            code=$(curl -sS --max-time 25 -o /tmp/b -w '%{http_code}' -H 'accept: application/json,*/*' -H 'referer: https://stock.naver.com/' -A 'Mozilla/5.0' "$url" || true)
            echo "HTTP $code"
            head -c 12000 /tmp/b || true
            echo
          done
